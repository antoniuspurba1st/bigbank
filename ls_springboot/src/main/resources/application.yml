server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ddbank
    username: postgres
    password: 123123

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
